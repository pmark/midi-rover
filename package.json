{
  "name": "midi-rover",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy:pages": "pnpx wrangler pages deploy dist",
    "test": "node --test --experimental-strip-types --experimental-specifier-resolution=node src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/three": "^0.183.1",
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "midi-file": "^1.2.4",
    "simplex-noise": "^4.0.3",
    "soundfont-player": "^0.12.0",
    "three": "^0.183.2"
  }
}
